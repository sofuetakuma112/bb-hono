import type { Meta, StoryObj } from '@storybook/react';
import Upload from '../upload';

export default {
  component: Upload,
} as Meta<React.ComponentProps<typeof Upload>>;

export const Basic: StoryObj<React.ComponentProps<typeof Upload>> = {};
